<%= render 'shared/backend/list_nav', klass: SongPoem %>

<div class="container-fluid">
  <div class="row">
    <div class="col-md-12">
      <%= render 'shared/backend/search_field', q: @q %>
      <%= render 'shared/backend/work_list', q: @q, works: @song_poems %>

      <%= paginate @song_poems %>
    </div>
  </div>
</div>
