<div class="d-flex justify-content-between align-items-baseline">
  <h2><%=I18n.t 'pages.show', model: h(HanPoetry), scope: :views %></h2>
  <div class="mr-3">
    <%= link_to '编辑', [ :edit, :backend, @han_poetry ] %>
  </div>
</div>
<hr>
<br>

<div class="container">
  <div class="row my-3">
    <div class="col-md-8 offset-md-2 d-flex flex-column align-items-center">
      <h1 class="text-success"><%= @han_poetry.title %></h1>
      <br>
      <div class="text-muted"><%= @han_poetry.dynasty %> - <%= @han_poetry.author %></div>
      <br>
      <div class="font-weight-normal">
      <% @han_poetry.content.each do |line| %>
        <p class="poem-body"><%= line %></p>
      <% end %>
      </div>
    </div>
  </div>
</div>

<h2 class="category"><%= h Work, :notes %></h2>
<div class="mx-5">
  <% if @han_poetry.notes.empty? %>
    <h5 class="font-weight-normal">暂无, <%= link_to '我来补充', [ :edit, :backend, @han_poetry ] %></h5>
  <% else %>
    <ul class="list-group list-group-flush">
      <% @han_poetry.notes.each do |line| %>
        <li class="list-group-item"><%= line %></li>
      <% end %>
    </ul>
  <% end %>
</div>

<h2 class="category"><%= h Work, :translate %></h2>
<div class="mx-5">
  <% if @han_poetry.translate.empty? %>
    <h5 class="font-weight-normal">暂无, <%= link_to '我来补充', [ :edit, :backend, @han_poetry ] %></h5>
  <% else %>
    <% @han_poetry.translate.each do |line| %>
      <p><%= line %></p>
    <% end %>
  <% end %>
</div>

<% if @han_poetry.translate_res.present? %>
  <h2 class="category"><%= h Work, :translate_res %></h2>
  <div class="mx-5">
    <% @han_poetry.translate_res.each do |line| %>
      <div class="small text-muted"><%= line %></div>
    <% end %>
  </div>
<% end %>
